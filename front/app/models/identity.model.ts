export class IdentityModel {
    id: number;

    username: string;

    fullname: string;

    isDomain: boolean;

    canGenereateMobileCert: boolean;

    canGenereateCertOnToken: boolean;

    isDemoMode: boolean;
}
